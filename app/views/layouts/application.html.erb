<!DOCTYPE html>
<html lang="en">
  <head>
    <title>CEDAR Admin</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="icon" href="favicon.ico" />
    <link rel="apple-touch-icon" href="apple-touch-icon.png" />
    
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'tag-cloud', 'data-turbolinks-track': 'reload' %>
    
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <a href="#main-content" class="a11y-nav" data-turbolinks="false">Skip to main content</a>
    <%= render "partials/ahrq_header" %>
    <%= render "partials/cedar_secondary_nav" %>
    <% if defined?(@subnavigation) %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top" aria-label="page nav menu">
        <span class="navbar-brand">Jump to: </span>
        <ul class="navbar-nav">
          <% @subnavigation.each do |item| %>
            <li class="nav-item">
              <a class="nav-link" href="#<%= item.downcase %>" data-turbolinks="false"><%= item %></a>
            </li>
          <% end %>
        </ul>
      </nav>
    <% end %>
    <% flash.each do |key, message| %>
      <div class="alert <%= flash_class(key) %> alert-dismissible fade show text-center" role="alert">
        <%= message %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% end %>
    <div class="container mt-4" role="main">
      <section id='main-content'>
        <%= yield %>
      </section>
    </div>
  </body>
</html>
